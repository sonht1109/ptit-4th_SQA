/*
 * Supplier  Messages
 *
 * This contains all the text for the Supplier component.
 */

import { defineMessages } from "react-intl";

export const scope = "Supplier";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
