/*
 * Order  Messages
 *
 * This contains all the text for the Order component.
 */

import { defineMessages } from "react-intl";

export const scope = "Order";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
